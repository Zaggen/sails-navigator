expect = require('chai').expect
navigator = require('../index')

describe 'navigator', ->
  it 'should be an object', ->
    expect(navigator).to.be.an('object')
  it 'should be an object', ->
    expect(navigator).to.be.an('object')